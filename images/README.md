Place your field reference images here. Recommended filenames:

- `gv_id.png` — screenshot of the voucher ID input field (or a stable label near it).
- `pin.png` — screenshot of the PIN input field.

Do NOT commit real voucher images or a file containing voucher data. Add this folder to `.gitignore` if you keep private screenshots here.